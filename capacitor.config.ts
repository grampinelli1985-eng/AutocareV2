import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.autocareia',
  appName: 'AutoCare IA',
  webDir: 'dist',
  server: {
    cleartext: false
  }
};

export default config;
